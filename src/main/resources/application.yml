server:
  port: 8080
  servlet:
    context-path: /v1
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dcim_tzy?characterEncoding=utf-8&useSSL=false
    username: root
    password: 999999
    driver-class-name: com.mysql.jdbc.Driver
mybatis-plus:
  type-aliases-package: com.example.demo.bean
  mapper-locations: classpath:mappers/*.xml